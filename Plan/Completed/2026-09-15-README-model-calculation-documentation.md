# README 模型计算说明工作计划

状态：`COMPLETED`

## 目标

依据当前代码、器件数据和固定校准配置，在根目录 `README.md` 中准确说明 C2M0025120D 的 M0–M3 模型，包括适用层级、输入参数、核心公式、校准方式、输出以及模型边界。

## 执行步骤

1. 核对统一 API、输入输出数据结构与单位。
2. 逐项核对 M0、M1、M2、M3 的实际计算公式与阶段逻辑。
3. 核对器件曲线、寄生参数和版本化 CalibrationProfile 的使用方式。
4. 将模型说明写入 `README.md`，确保公式与实现一致。
5. 运行文档检查和完整测试，确认 README 链接、代码示例及模型回归正常。
6. 将本计划状态更新为 `COMPLETED` 并移入 `Plan/Completed/`，确保 `Plan/Active/` 为空。

## 完成记录

- 根 README 已补充 M0–M3 的输入、数据关系、核心公式、固定校准、输出契约和模型边界。
- 已增加 Plan 工作流说明和“Active 最多一个 Markdown plan”的自动测试。
- 18 项单元、回归和验证测试全部通过。
- README 中 12 个相对链接全部解析成功。
